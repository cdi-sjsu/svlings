# build output from svlings / verilator
.svlings-build/
obj_dir/
*.vcd
*.fst
*.log

# direnv's local cache (the .envrc itself is tracked and just says "use flake")
.direnv/

# editor/OS noise
.DS_Store
*.swp

# local Claude Code session state, not part of the course
.claude/

