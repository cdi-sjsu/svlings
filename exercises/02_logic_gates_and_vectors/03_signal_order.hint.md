One line, the same shape as the two already there:

```systemverilog
assign c_inverted = ~c;
```

The point of the exercise is where you put it - which is: anywhere.
Try it above `assign y = ...` and convince yourself the result doesn't
change.
